# Read about fixt   ures at http://ar.rubyonrails.org/classes/Fixtures.html

a_test_pattern_system:
   name: A Great pattern system for doing stuff!
   author: Bob le poulpe
   root_pattern_id: <%= Fixtures.identify(:first_pattern) %>
   participant_id: acheteur, vendeur

a_new_pattern_system:
    name: Un système de patrons sans son root_pattern
    author: Bob le poulpe
    root_pattern_id:
   # t.string   "author"
   # t.string   "name"
   # t.string   "root_pattern_id"
   # t.datetime "created_at"
   # t.datetime "updated_at"
   # t.integer  "participant_id"
   # t.text     "characteristics"
   # t.text     "update_field"

# two:
#   column: value
