%h1 Editing pattern system
- form_for(@pattern_system) do |f|
	= f.error_messages

	= render :partial => 'form', :locals => {:f => f}

	%p
		%br
		= f.submit "Update"
	
= link_to 'Show', @pattern_system