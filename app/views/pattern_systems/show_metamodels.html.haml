%h1 Creation of a new pattern system (step 1/2)
- form_for(@pattern_system) do |f|
  = f.error_messages
  %table
    %tr
      %td= f.label :author, t(:author)
      %td= f.text_field :author 
    %tr
      %td=  f.label :name, t(:name)
      %td=  f.text_field	:name
    %tr

      %td= f.label :system_formalism, "System metamodel"
      %td= f.collection_select(:system_formalism_id, @metamodels, :id, :name, {:prompt => true})

  = f.submit 'Next'
