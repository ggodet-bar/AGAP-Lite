%h1 Editing pattern formalism

= form_for([@system_formalism, @pattern_formalism]) do |f|
  = f.error_messages
  = render :partial => 'form', :locals => {:f => f}
  .commands
    %p
      = f.submit t(:update)
      |
      = link_to t(:back), [@system_formalism, @pattern_formalism]
